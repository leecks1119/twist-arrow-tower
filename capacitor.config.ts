import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.chanhee.arrowescape.web',
  appName: 'Arrow Escape Web',
  webDir: 'dist'
};

export default config;
